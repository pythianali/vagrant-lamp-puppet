---
  databases:
    CAdb:
      name: 'CAdb'
      user: 'CAuser'
      password: 'CApass'
      host: 'localhost'
      grant: 
       - DELETE
       - INSERT
       - SELECT
       - UPDATE
       - CREATE

